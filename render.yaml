services:
  - type: web
    name: payharness
    runtime: node
    rootDir: payharness
    buildCommand: npm install && npm run build
    startCommand: npm run start:prod
    envVars:
      - key: NODE_VERSION
        value: 20.11.1
